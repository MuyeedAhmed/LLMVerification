Implement a function: int json_object_update(json_t *object, json_t *other) 
such that it 
Update *object* with the key-value pairs from *other*, overwriting
   existing keys. Returns 0 on success or -1 on error.